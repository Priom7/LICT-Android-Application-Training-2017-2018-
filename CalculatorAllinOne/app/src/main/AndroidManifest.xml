<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sapriom.calculatorallinone">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StartMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ListMainActivity" />
        <activity android:name=".CalculatorMainActivity" />
        <activity android:name=".CgpaMainActivity" />
        <activity android:name=".FeeMainActivity" />
        <activity android:name=".CoolDevidativeMainActivity" />
        <activity android:name=".TriMainActivity" />
        <activity android:name=".ThnkMainActivity"></activity>
    </application>

</manifest>